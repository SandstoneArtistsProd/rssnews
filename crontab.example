# Example Crontab Configuration for Deadline Collector
#
# Installation:
# 1. Edit this file and update the paths
# 2. Run: crontab -e
# 3. Copy the desired line(s) below into your crontab
#
# Note: Replace /path/to/deadline-collector with actual path

# Run every hour at :00 minutes
0 * * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1

# Run every 30 minutes
*/30 * * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1

# Run every 2 hours
0 */2 * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1

# Run every 6 hours (4 times a day)
0 */6 * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1

# Run daily at 9 AM
0 9 * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1

# Export to CSV daily at midnight
0 0 * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py --export >> logs/export.log 2>&1

# Example: Collect every hour and export daily
0 * * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py >> logs/cron.log 2>&1
0 1 * * * cd /path/to/deadline-collector && /path/to/deadline-collector/venv/bin/python collector.py --export >> logs/export.log 2>&1

# To view current crontab:
# crontab -l

# To edit crontab:
# crontab -e

# To remove all cron jobs:
# crontab -r

# View cron logs (Ubuntu/Debian):
# grep CRON /var/log/syslog

# View cron logs (CentOS/RHEL):
# grep CRON /var/log/cron
